/*const assert = require('assert');
const { getFileContents } = require('./utils');

describe('Your script', () => {
  it('should have the suggested command added correctly', async () => {
    const scriptFile = await getFileContents('../insert_data.sh');

    assert(/then[\s\S]*INSERT_STUDENT_RESULT[\s\S]*if[ \t]+\[\[[ \t]+\$INSERT_STUDENT_RESULT[ \t]+==[ \t]+('|")INSERT 0 1\1[ \t]\]\]([ \t]*;\s*|\s*\n\s*)then\s+echo[ \t]+('|"|)Successfully inserted \$FIRST \$LAST into students\3([ \t]*;\s*|\s*\n\s*)fi[\s\S]*fi/gm.test(scriptFile));
  });
});*/
