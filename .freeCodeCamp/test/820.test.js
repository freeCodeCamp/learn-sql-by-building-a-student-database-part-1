/*const assert = require('assert');
const { getLastLog } = require('./utils');

describe('You', () => {
  it('should enter the suggested command', async () => {
    const lastLog = await getLastLog(true);
    const re = /^statement:truncate(studentscascade|students,gpas,students_majors|students,students_majors,gpas|gpas,students,students_majors|gpas,students_majors,students|students_majors,students,gpas|students_majors,gpas,students);/i;

    assert(re.test(lastLog) && !/ERROR/g.test(lastLog));
  });
});*/
